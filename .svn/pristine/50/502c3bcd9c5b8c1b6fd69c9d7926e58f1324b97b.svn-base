#define HW_MODULE "G5"

#define DEFAULT_CONFIGFILE "/etc/koala.conf"
#define KOALA_VERSION_PATH "/etc/version/kversion"
#define MAX_BUF 4096

#define KOALA_REBOOT "koala_reboot"

#define VERSION_SIZE 16
#define CHECK_SEC 10;

#define M_VERSION "1.3.0002"

#define LOCAL_INT "br-lan1"

#define UCI_CONFIG_FILE "wifidog"

typedef enum ctrl_svr_stat_t_ {
    CONNECTED_FAILED = -3,
    SOCKET_ERR,
    DNS_ERR
} t_ctrl_svr_stat;

typedef struct _ctrl_server_url_t {
    char   *ctrl_url;
    struct _ctrl_server_url_t *next;
} t_ctrl_server_url;
